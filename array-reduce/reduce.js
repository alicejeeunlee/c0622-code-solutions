function reduce(array, reducer, initialValue) {
  const previousValue = initialValue;
  const index = 1;
  const currentValue = array[index - 1];
  // for (let i = 0; i < array.length; i++) {

  // }
}
