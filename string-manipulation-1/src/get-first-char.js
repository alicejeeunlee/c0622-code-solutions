/* exported getFirstChar */

/*
return first character of string
2 ways to access an individual character in a string
  charAt() method
  treat the string as an array-like object | square bracket notation
*/

function getFirstChar(string) {
  return string.charAt(0);
  // return string[0];
}
